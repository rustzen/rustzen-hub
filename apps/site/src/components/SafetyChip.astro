---
interface Props {
  level: 'safe' | 'caution' | 'danger';
  label?: string;
  class?: string;
}

const { level, label, class: className, ...rest } = Astro.props;
const cls = ['safety-chip', level, className].filter(Boolean).join(' ');
---

<span class={cls} {...rest}>{label ?? level}</span>
